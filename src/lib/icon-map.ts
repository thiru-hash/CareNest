import { LayoutDashboard, Calendar, Users, UsersRound, Building2, ShieldAlert, Settings, type LucideIcon } from "lucide-react";

export const iconMap: { [key: string]: LucideIcon } = {
    LayoutDashboard,
    Calendar,
    Users,
    UsersRound,
    Building2,
    ShieldAlert,
    Settings,
};

export const iconNames = Object.keys(iconMap);
